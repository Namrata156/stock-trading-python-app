# import logging

# logging.basicConfig(filename='stock_pipeline.log', level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

# logging.info(f"Fetching page {page_number} with URL: {data['next_url']}")
# logging.warning("Rate limit reached, retrying...")
# logging.error("Pagination failed after multiple retries")
